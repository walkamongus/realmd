---
realmd::krb_client_package_name: krb5-user
realmd::required_packages:
  sssd-tools:
    ensure: installed
  libpam-modules:
    ensure: installed
  libnss-sss:
    ensure: installed
  libpam-sss:
    ensure: installed
  samba-common-bin:
    ensure: installed
